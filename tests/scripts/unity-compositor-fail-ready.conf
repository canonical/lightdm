#
# Check quits when the compositor fails to indicate it is ready
#

# Increase timeout as LightDM waits a long time for the system compositor to start
[test-runner-config]
timeout=10

[SeatDefaults]
type=unity

#?RUNNER DAEMON-START

# System compositor starts but doesn't indicate it is ready
#?UNITY-SYSTEM-COMPOSITOR START

# Cleanup
#?UNITY-SYSTEM-COMPOSITOR TERMINATE SIGNAL=15

# Daemon stops with error
#?RUNNER DAEMON-EXIT STATUS=1
